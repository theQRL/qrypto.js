{
  "name": "root",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "FORCE_COLOR=1 turbo test",
    "lint": "turbo lint-check",
    "report-coverage": "turbo report-coverage"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.11.3"
  }
}
